# install with `conda env create -bana6043.yaml`
# activate with `conda activate bana7085`

name: bana7085
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - jupyter
  - nbformat
